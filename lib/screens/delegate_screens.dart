import 'package:flutter/material.dart';

class DelegateDashboardScreen extends StatelessWidget {
  const DelegateDashboardScreen({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Dashboard Délégué')));
}

class DelegateClassScreen extends StatelessWidget {
  const DelegateClassScreen({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Classe Délégué')));
}

class DelegateDocsScreen extends StatelessWidget {
  const DelegateDocsScreen({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Documents Délégué')));
}

